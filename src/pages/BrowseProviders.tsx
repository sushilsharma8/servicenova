import { ServiceProviderList } from "@/components/service-provider/ServiceProviderList";

const BrowseProviders = () => {
  return (
    <div className="container mx-auto py-8">
      <ServiceProviderList />
    </div>
  );
};

export default BrowseProviders;